package ch1;

public class Constructors {
	
	// an example of a constructor
	
	// a default constructor looks like this, it has no return type
	// it also has the same name as the class. A constructor creates an object
	// based on a class 
	public void Constructors() {
		
	}
	
	// to create an instance of a class, call the class name with the new keyword
	// like this
	// this is five separate parts
	// Constructors in the type of value to store on the left
	// c is the name of that variable
	// = is the assignment operator
	// new is a keyword that creates an instance of the object
	// and Constructors() calls the constructor for the Constructors class
	Constructors c = new Constructors();
	
	
	
}
